import 'package:flutter_test/flutter_test.dart';

void main() {
  group('Pruebas Básicas de ExperienceProvider', () {
    test('placeholder - requiere Firebase.initializeApp()', () {
      expect(true, isTrue);
    });
  });
}
